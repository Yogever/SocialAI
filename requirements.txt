fastapi==0.115.6
uvicorn[standard]==0.34.0

# LLM planner (the model call is behind an abstraction; the model itself is a
# runtime config knob, PLANNER_MODEL, today pointing at a local Ollama model).
langchain==1.3.14
langchain-ollama==1.1.0
